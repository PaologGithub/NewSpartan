<Page
    x:Class="NewSpartan.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewSpartan.App"
    xmlns:controls="using:NewSpartan.App.UI.Controls"
    xmlns:renderer="using:NewSpartan.EdgeWebviewRenderer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <!-- Titlebar -->
            <RowDefinition x:Name="TitlebarRow" Height="32" />
            <!-- ControlBar -->
            <RowDefinition x:Name="ControlBarRow" Height="40" />
            <!-- WebRenderer -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:Titlebar x:Name="TitlebarControl" Grid.Row="0" />
        <controls:ControlBar x:Name="ControlBarControl" Grid.Row="1" />
        <renderer:WebRenderer x:Name="WebRendererControl" Grid.Row="2" />
    </Grid>
</Page>
